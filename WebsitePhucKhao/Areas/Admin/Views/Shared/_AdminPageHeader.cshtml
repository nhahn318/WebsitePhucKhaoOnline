@model dynamic

<div class="admin-page-header mb-4">
    <div class="d-flex justify-content-between align-items-center">
        <div>
            <h4 class="page-title mb-1">@ViewData["Title"]</h4>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Dashboard", new { area = "Admin" })">Trang chủ</a></li>
                    <li class="breadcrumb-item active">@ViewData["Title"]</li>
                </ol>
            </nav>
        </div>
        <div>
            @if (ViewData["ShowCreateButton"] as bool? ?? true)
            {
                <a href="@Url.Action("Add", "SinhVien", new { area = "Admin" })" class="btn btn-primary">
                    <i class="fas fa-plus me-2"></i>Thêm mới
                </a>
            }
        </div>
    </div>
</div> 